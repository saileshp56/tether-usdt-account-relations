{
  "name": "tether-usdt-account-relations",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ tether-usdt-account-relations",
    "create-local": "graph create --node http://localhost:8020/ tether-usdt-account-relations",
    "remove-local": "graph remove --node http://localhost:8020/ tether-usdt-account-relations",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 tether-usdt-account-relations",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.58.0",
    "@graphprotocol/graph-ts": "0.30.0",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.8.2",
    "matchstick-as": "0.5.0"
  }
}
